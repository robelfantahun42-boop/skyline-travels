{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "destination": "server.js" },
    { "src": "/(.*)", "destination": "/$1" }
  ]
}
